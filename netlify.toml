[build]
  command = "npm run build"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

# Angular SSR adapter — empacota o app SSR como Netlify Function automaticamente.
[[plugins]]
  package = "@netlify/angular-runtime"
